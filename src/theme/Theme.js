import { createTheme } from '@mui/material/styles';
const themeConecta = createTheme({
    palette: {
      primary: {
        main: '#FF0000',
      },
      secondary: {
        main: '#00FF00',
      },
    },
    // Otras propiedades del tema, como tipografía, espaciado, etc.
  });
  
  export default themeConecta;
  